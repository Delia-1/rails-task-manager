<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p><br>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%><br>

<p><%= link_to "edit task", edit_path(@task) %></p>

<%= link_to "index-task", tasks_path %>
